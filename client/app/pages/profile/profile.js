'use strict';

/**
 * Login controller
 *
 * @author Øyvind Hellenes
 */

angular.module('corsaneApp')
	.controller('ProfileCtrl', ['$scope', '$location','$log','authService','$cookieStore','api', '$state', '$rootScope','global',
		function($scope, $location, $log, authService, $cookieStore, api, $state, $rootScope, global) {

            
		}
	]);